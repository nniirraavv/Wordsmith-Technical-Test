export enum HttpMethodsType {
    get = "get",
    post = "post",
    put = "put",
    delete = "delete",
    patch = "patch"
}

export enum ResponseStatus {
    ok = "ok",
    error = "error"
}

export enum SortOrder {
    ASC = 'ASC',
    DESC = 'DESC'
}

export enum AccountAllowedSortingFields {
    id = 'id',
    name = 'name',
    email = 'email',
    createdAt = 'createdAt',
    updatedAt = 'updatedAt'
}